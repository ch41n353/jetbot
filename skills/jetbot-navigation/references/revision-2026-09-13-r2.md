# Revision 2 — 2026-09-13

The new `/home/jetbot/jetbot/local_nav/route_executor.py` follows a planner-supplied
straight waypoint batch without model calls or intermediate waypoint stops.
Read `/home/jetbot/jetbot/local_nav/ROUTES.md` for plan schema and commands.
`route_geometry.py` checks the entire conservative chassis corridor against
explicitly inspected free space and retained obstacle rectangles in a fixed
initial-lens frame. It does not infer free space from missing detections.

Limits remain 15 cm total / two seconds powered / duty 0.16 with bounded steering.
Runtime uses signed forward camera/IMU odometry, a lateral/heading envelope,
tracking/tilt/timing guards and a stall watchdog. No turn batches or new obstacle
detection. Axle offset remains unmeasured. User preview preference still applies.

Service responses now include session/control generation tokens. The new executor
renews with these tokens, so `stop` invalidates it. Legacy controllers remain
non-latching and require shutdown for reliable cancellation. Motor leases remain
200 ms; no blind background renewal was introduced.

Evidence: 48 offline tests passed; simulated three-waypoint execution made two
hold commands then one final stop, with no intermediate stop. Live stationary
preflight drift 0.0274 cm. Live disarmed stale-token rejection passed. No powered
trial or measured speedup yet. Do not present simulated behavior as hardware proof.
The original baseline reference and manifest are unchanged.
