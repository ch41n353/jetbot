# Revision 7 — 2026-09-14, live return and estimator continuity

Live return report: `/home/jetbot/jetbot/reports/2026-09-14-return-trial.md`.
After two failed attempts and fixes, a real 19 cm approach executed both local
segments with **zero intermediate planner calls**, 4.023 seconds command-to-result.
It measured 21.309 cm, so outcome was `stopped / Batch overshot final distance`.
Robot ended approximately 17–20 cm from the Advil and service shut down. This
validates physical local continuation, not accurate arrival or general autonomy.

Batches now preserve a single IMU attitude timeline and the original tilt
reference across segments. Fresh initialization from a recent braking sample
had caused a 4.919° apparent starting inclination and a safe pre-motion stop.
Continuous replay passed; initialization and tilt limits were not weakened.
Verified stopped poses at internal markers can update remaining distance even
when the segment reports overshoot/undershoot. Original map, full chassis,
uncertainty and final ±1 cm checks still apply. Unverified motion never continues.
Last verified pose remains in the result when the final tolerance fails.

Ordinary route plans additionally accept `travel_direction: "reverse"`, using
positive travel waypoints up to 15 cm and at most two seconds powered. Physical
reverse swept rectangle is `[-16, -distance-28, 16, 9]` cm in the original lens
frame. It includes rear chassis and braking. Require inspected/retained rear
clearance; do not infer it from the front camera. This is a supervised reposition
option, not automatic reverse recovery or a reverse batch. Motor power stays
0.16 with bounded steering; all leases, progress and sensing checks remain.
Results preserve physical x/z coordinates, so reverse position z is negative.
See `/home/jetbot/jetbot/local_nav/SESSIONS.md` for commands and result semantics.
