# Revision 6 — 2026-09-14, object approaches with fewer planner calls

Prioritize complete object approaches and end-to-end elapsed time. A live Advil
approach ended at roughly 14–16 cm projected clearance, but took 219 seconds from
first to final capture; drive commands plus turn controller accounted for only
10.52 seconds. Planning/tool gaps dominated. First route's post-stop tracking
failed; fresh imagery allowed replanning. Report and logs:
`/home/jetbot/jetbot/reports/2026-09-14-advil-approach.md`.

`maneuver_session.py` now accepts `plan_approach`: exact last-capture image path,
object carpet-contact pixel, explicitly inspected free rectangle and retained
obstacle rectangles. It prepares the plan and full-corridor preview together.
Show the returned preview before execute. It does not infer clearance from pixels.
Near targets return an estimated standoff assessment; off-axis targets require
alignment. Read `/home/jetbot/jetbot/local_nav/SESSIONS.md` for all fields.

Opt-in `batch: true` on both planning and execution uses `approach_batch.py` for
up to 30 cm of straight approach. Preview the entire batch once. Local segments
remain at most 15 cm / two seconds powered and stop for verification between
segments, without VLM calls. The initial map persists; transformed full-chassis
corridors, accumulated uncertainty, generation tokens, and pose are checked.
An external stop at a segment boundary cancels the batch. Failed/unverified
segments terminate it and return `requires_planner: true`. There are no automatic
model API calls. Do not describe this as continuous motor motion through stops.

This is for an inspected static scene. No dynamic obstacle recognition, target
tracker, turning batch, or autonomous recovery was added. Never represent a
30 cm batch as having the same observation risk as one short supervised segment.
Existing operation authorization and user stop behavior apply. Do not make routine
planner calls at internal segment boundaries when the whole batch was inspected
and previewed; reserve high-level decisions for completion or exceptions.

Validation: direct approach preparation tested against recorded approach pixels;
actual final image tested with motors disarmed and correctly yielded no new plan.
Production batch simulation covers completion without intermediate planner calls,
retained obstacle and unknown-space rejection, and cancellation between segments.
Full batch physical validation and end-to-end speedup remain unmeasured. Do not
claim the earlier live approach used this newly implemented batch mode.

Final checks: 75 tests passed and nine production-controller simulations saved
in `/home/jetbot/jetbot/reports/2026-09-14-batch-validation.json`. Four of five
normal motor settings completed; higher coasting and all four injected fault
cases stopped. Continuation is anchored to the exact verified settled frame,
not a newer unregistered snapshot; one-centimeter repositioning prevents restart.
