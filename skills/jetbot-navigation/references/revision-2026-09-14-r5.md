# Revision 5 — 2026-09-14, orchestration speed

The user clarified that end-to-end speed is the priority, then explicitly
confirmed the robot was unplugged and free to move. One live 15 cm route ran.
Do not spend future speed iterations optimizing stopping precision alone.

New tools:

- `/home/jetbot/jetbot/local_nav/maneuver_session.py`: persistent sensing session;
  JSON capture/status/execute/stop/shutdown commands; responsive stop during a
  route worker; cleanup on exit; refuses to replace an existing service.
- `/home/jetbot/jetbot/local_nav/route_preview.py`: standard straight-route preview
  from a fresh image, with full chassis/clearance envelope and a top-down inset.
  Uses `local_nav/assets/straight_preview.html`. It does not check obstacles.

Read `/home/jetbot/jetbot/local_nav/SESSIONS.md` for commands and timing semantics.
Keep sensing warm during a motion batch, then shut down. Show the returned preview
before execution and inspect people/objects in the fresh image. A generated preview
is not a checked free-space map. Do not publish or execute a candidate merely
because its diagram was generated automatically.

Live evidence: one 15 cm request took 3.021 seconds command-to-result, including
1.380 seconds powered, with no model calls between waypoints. It ended at an
estimated 16.377 cm and correctly reported overshoot. The initial capture-to-first
controller frame took 72.619 seconds, including one-time preview/code preparation.
Automatic capture/preview generation then took 0.050–0.053 seconds locally; model
inspection and tool latency are excluded. The next candidate was held because a
person entered/approached the corridor. No second route ran. Service shut down.

Tests cover disarmed execution rejection and processing a stop while the route
worker is running, including multiple commands in one input chunk. The final
preview integration was added after the first live session, so a complete live
steady-state timing comparison remains outstanding. No turn-control changes or
expanded driving bounds were introduced in this revision.
